<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.domain.login.repository.mapper.LoginMapper">
  <select id="findByUsername" resultType="com.example.demo.domain.register.model.User">
    SELECT seqNumber, name, email, password, admin, deleteFlag, createDate, updateDate 
    FROM eunjun.user
    WHERE email = #{email} AND password = #{password}
  </select>

</mapper>